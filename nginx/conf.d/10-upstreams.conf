upstream navidrome_backend {
    zone navidrome_backend 64k;
    server navidrome:4533 max_fails=3 fail_timeout=30s;
    keepalive 128;
    keepalive_time 1h;
    keepalive_timeout 60s;
    keepalive_requests 10000;
}
